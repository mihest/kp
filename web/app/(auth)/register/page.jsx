import ClientComponent from "@/app/(auth)/register/ClientComponent";

export const metadata = {
    title: 'Register',
}

const RegisterPage = () => {
    return <ClientComponent />
}
export default RegisterPage;